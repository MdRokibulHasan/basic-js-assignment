// 1.feet to inch
/* function feetToInch(feet) {
    if (feet >= 0) {
        let inch = feet * 12;
        return inch;
    }
    else {
        return 'invalid Input';
    }

}
const result = feetToInch(10);
console.log(result);
2.

function feetToInch(feet) {
    if (feet >= 0) {
        let inch = feet * 12;
        return inch;
    }
    else {
        return 'invalid Input';
    }

}
const result = feetToInch(10);
console.log(result);
 */
// 3.papper requriement 

/* function paperRequirement(book1, book2, book3) {
    if (book1 >= 0 && book2 >= 0 && book3 >= 0) {
        let pageOfFirstBook = book1 * 100;
        let pageOfSecondtBook = book2 * 200;
        let pageOfThirdBook = book2 * 300;
        const totalPageOfBook = pageOfFirstBook + pageOfSecondtBook + pageOfThirdBook;
        return totalPageOfBook;
    }
    else {
        return 'invalid input';
    }

}
const totalPages = paperRequirement(5, 6, 3);
console.log(totalPages); */

// 4. best friend 

/* function bestFriend(names) {
    let largestName = names[0]
    for (item of names) {
        if (item.length > largestName.length) {
            largestName = item;
        }
    }
    return largestName;


}
const names = ['anik', 'anim', 'suha', 'duha', "shakill"];
const largeName = bestFriend(names);
console.log(largeName); */

// 5.Positive number 
/* const numbers = [25, 65, -5, 369, 56, -65];
for (item of numbers) {
    if (item > 0) {
        console.log(item);
    }
    else {
        break;
    }
} */

6.

cl